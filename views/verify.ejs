<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
      <form action="/verify" method="post" style="display: flex; flex-direction: column; gap: 15px; margin-top: 20px;">
    <input 
      type="email" 
      name="email" 
      value="<%= email %>" 
      placeholder="Email" 
      required 
      readonly 
      style="padding: 10px; font-size: 16px;"
    >
    <input 
      type="text" 
      name="code" 
      placeholder="6-digit code" 
      required 
      maxlength="6"
      style="padding: 10px; font-size: 16px; text-align: center;"
    >
    <button type="submit" style="padding: 10px; font-size: 16px; cursor: pointer;">Verify</button>
  </form>

  <p style="margin-top: 20px;">
    Didn't receive the code? <a href="/resend">Resend Code</a>
  </p>
</div>
</body>
</html>